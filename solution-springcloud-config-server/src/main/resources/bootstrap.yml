spring:
  application:
    name: config-single-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/AcMessi/springcloud-config.git #配置文件所在仓库
          username: AcMessi #登录账号
          password: Acn.123!@# #登录密码
          default-label: master #配置文件分支
          search-paths: asset  #配置文件所在根目录